<?xml version="1.0" encoding="UTF-8"?>
<messaging-deployment xmlns="urn:jboss:messaging-deployment:1.0">
	<hornetq-server>
		<jms-destinations>
			<jms-queue name="${app.context}DocumentURLQueue">
                <entry name="${document.url.queue}"/>
            </jms-queue>
            <jms-queue name="${app.context}ShowQueue">
                <entry name="${show.queue}"/>
            </jms-queue>
		</jms-destinations>
	</hornetq-server>
</messaging-deployment>